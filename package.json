{
  "name": "recoverwwatch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install --production=false && cd ../client && npm install --production=false && npm run build",
    "start": "cd server && node index.js",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  }
}
